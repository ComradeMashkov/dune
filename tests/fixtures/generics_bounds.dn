import array;
import math;

// Generic functions are instantiated only for call sites.
fn identity<T>(value: T) -> T {
    return value;
}

fn twice<T: numeric>(value: T) -> T {
    return value + value;
}

let words: [text] = ["dune", "lang"];
let reversed: [text] = array.reverse(words);
let rough: real32 = 1.5;

print(identity(42));
print(identity("ok"));
print(reversed.first());
print(math.square(12));
print(math.square(rough));
print(twice(9));
