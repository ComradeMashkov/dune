import io;
// ERROR FIXTURE: this file is expected to FAIL at runtime.
// Pull in the stdlib matrix module (provides vector and its dot product).
import matrix;

// Build `left`: a vector of length 2 (components [1, 2]).
left = matrix.vector([1, 2]);
// Build `right`: a vector of length 3 (components [1, 2, 3]).
right = matrix.vector([1, 2, 3]);

// Dot product requires both vectors to have the SAME length. Here left has 2
// components and right has 3, so their shapes do not match.
// THIS LINE CAUSES THE ERROR: vector shape mismatch (length 2 vs length 3) in dot.
io.println(left.dot(right));
