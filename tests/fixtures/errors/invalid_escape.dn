// ERROR FIXTURE: this file is expected to FAIL at lex/parse time.
// Bind `bad` (annotated as text) to a text literal that contains the escape
// sequence \x. Dune's text literals only allow a fixed set of escape codes
// (such as \n, \t, \\, \"); \x is NOT a recognized escape.
// THIS LINE CAUSES THE ERROR: invalid text escape sequence "\x".
bad: text = "\x";
// Never reached: the file fails while scanning the string literal above, so
// this print of `bad` never executes.
print(bad);
