import log;
import io;

log.set_level(log.DEBUG);
log.trace("hidden trace");
log.debug("cache miss");
log.info("building target");
log.warn("using fallback config");
log.error("failed");
io.println("stdout-ok");
