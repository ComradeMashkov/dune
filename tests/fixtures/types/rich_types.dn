fn add_real(left: real, right: real): real {
    return left + right;
}

small: u8 = 250;
large: uint64 = 10000000000;
total: uint64 = large + 5;
ratio: real = add_real(1.25, 2.5);
mark: glyph = 'Q';

print(small);
print(total);
print(ratio);
print(mark);
